[flake8]
# ignore virtual environment and cache directory
exclude =
    .venv,
    __pycache__,
    tests/*

# code line max length
max-line-length = 120

# ignore some unnecessary warning
# E203, whitespace before ':', conflicts with black formatting
# W503  line break before binary operator, conflicts with black
ignore =
    E203,
    W503
